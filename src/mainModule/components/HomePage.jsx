import { Layout } from './Layout.jsx';

import './HomePage.scss';

export const HomePage = () => {
    return (
        <Layout>
           <div>
               Home Page
           </div>
        </Layout>
    );
}
